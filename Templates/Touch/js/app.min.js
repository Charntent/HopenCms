var p1 = 1;
var index = 1;
var pagedid = 0;
$(function(){
	$(window).scroll(function(){  
		var scrollTop = $(this).scrollTop();
	　　var scrollHeight = $(document).height();
	　　var windowHeight = $(this).height();
	　　if(scrollTop + windowHeight== scrollHeight){
	　　　　getmore();
	　　}	
	});  
});
function getmore(){
	switch(index){
		   case 1:
		      pagedid = p1;
		   break;
		}
		if(pagedid==0){ return false;}
	
	     $.post(moreUrl,{catid:catid,page:pagedid},function(data){
			data = data.replace(/(\s*$)/g, "");
			if(data==''){
				switch(index){
				   case 1:
					  p1 = 0;
				   break;
				}
				$("#pageLoading #loadingTitle").html('没有更多数据了.');
				return false;
			}
			
			$(cBox).append(data);	
			switch(index){
				   case 1:
					  p1++;
				   break;
				}
			$("#pageLoading #loadingTitle").html('下拉加载更多....');
		},'html');
}

